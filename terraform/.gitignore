# Terraform's local cache of providers/modules — big, regenerated by `init`.
.terraform/
.terraform.lock.hcl

# STATE FILES — never commit. They record real resource ids and can contain
# secrets (passwords, keys) in plaintext.
*.tfstate
*.tfstate.*

# Your real variable values (email, etc.)
terraform.tfvars
*.auto.tfvars
